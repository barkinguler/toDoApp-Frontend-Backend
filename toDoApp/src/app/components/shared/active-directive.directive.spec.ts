import {ActiveDirectiveDirective} from './active-directive.directive';

describe('ActiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
